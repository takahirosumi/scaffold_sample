<p id="notice"><%= notice %></p>

<h1>Rooms</h1>

<table>
  <thead>
    <tr>
      <th>Room type</th>
      <th>Num rooms</th>
      <th>Square</th>
      <th>Floor</th>
      <th>Building type</th>
      <th>Structure type</th>
      <th>Floor height</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.room_type %></td>
        <td><%= room.num_rooms %></td>
        <td><%= room.square %></td>
        <td><%= room.floor %></td>
        <td><%= room.building_type %></td>
        <td><%= room.structure_type %></td>
        <td><%= room.floor_height %></td>
        <td><%= link_to 'Show', room %></td>
        <td><%= link_to 'Edit', edit_room_path(room) %></td>
        <td><%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Room', new_room_path %>
